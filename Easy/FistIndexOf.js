const firstIndex = (haystack, needle) => {
    return haystack.indexOf(needle)
}

const firstIndexNeedle = (haystack, needle) => {
    const m = haystack.length, n = needle.length
    
}

console.log(firstIndex("sadbutsad", "sad"));
